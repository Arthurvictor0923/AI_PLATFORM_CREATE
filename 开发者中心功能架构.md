# 洛书·模型服务平台 · 开发者中心功能架构文档

> 版本：v1.3 ｜ 日期：2026-07-28 ｜ 依据：工作台（`workbench.html`）现有功能梳理 + 项目 CLAUDE.md 模块清单
> 变更：v1.2 移除「开发者社区 / 应用创空间 / 认证与成长」，开发者中心精简为 5 模块；v1.3 锁定核心原则——开发者中心为平台「顶层菜单（一级导航）项」，与首页/模型广场等平级，非工作台子页，落地于原注释态「开发中心」预留位；v1.4 明确镜像管理 / Notebook 实例管理归属工作台·开发中心，开发者中心仅提供「基础镜像目录（只读）」与「示例 Notebook + 一键启动」轻量入口，不重建管理功能；v1.5 移除「已注册应用（OAuth）」——系统仅到「推理服务」层级、无应用概念，个人凭证仅保留密钥 / 配额 / 权限 / 通知偏好；**v1.6 文档中心不再作为顶层菜单，改为内嵌于开发者中心左侧菜单的视图（iframe 载入 doc-center.html?embed=1，隐藏其自身顶部菜单与 footer），开发者中心模块由「文档与教程（轻量重组）」升级为完整的「文档中心」**。

---

## 0. 背景与问题

当前工作台（`workbench.html`）已承载开发者几乎所有的「动手」功能，可归纳为：

- **个人空间（快速导航）**：我的模型(12)、我的数据集(5)、我的需求、我的任务、我的申请、资源总览
- **开发中心**：镜像管理、在线开发（Notebook：代码编辑器 + 终端 + 运行/保存/快照）
- **创建能力（Quick Create）**：创建模型、创建推理服务(发布)、上传镜像、上传数据集、发布需求、启动 Notebook
- **运营闭环**：我的模型→发布（7 段流程：元数据→版本→镜像→服务类型→使用说明→推理配置→资源启动配置）、我的申请（数据/模型使用申请）
- **激励**：积分体系（1000 积分、3 次免费调用、积分规则）
- **平台概况**：187 模型 / 42 数据集 / 23 需求（只读统计）

但工作台本质是**个人操作台**（视角 = 「我的资源/任务」），存在两类能力缺口，导致平台能力难以被**外部开发者 / 第三方系统**消费与发现：

1. **能力消费层缺失**：无 API 密钥、无在线调试、无 SDK → 外部系统无法调用洛书的模型/推理服务。
2. **上手与学习缺失**：无快速开始、无教程/cookbook → 新开发者从 0 到首次成功成本高。

> 说明：**社区 / 生态类能力本期不纳入**（系统不做社区）；「应用创空间」与模型广场已发布的推理服务重叠，不单独建设。

> **结论**：开发者中心 = 工作台的**外围赋能层**，补齐上述缺口，而**不重复**工作台的运营功能。

---

## 1. 定位（核心原则：顶层菜单）

> ### 🔑 核心原则（必须锁定）
> **开发者中心是洛书模型服务平台的「顶层菜单（一级导航）项」，与首页 / 模型广场 / 案例中心 / 数据管理 / 需求市场平级。**（文档中心自 v1.6 起不再是顶层菜单，改为内嵌于开发者中心左侧菜单的视图。）
> 它不是工作台的子页面，也不挂载在「工作台 / 个人空间」之下；它是进入平台开发能力的**独立一级入口**。
> 项目顶导原本已预留该位置（`workbench.html` 中 `<span class="top-nav-item" data-nav="dev">开发中心</span>` 处于注释态），开发者中心即该预留位的正式落地。

- **一句话定位**：开发者中心是「让开发者发现平台能力、快速上手、并通过 API/SDK 消费能力的统一入口与赋能层」。
- **视角**：平台能给你**什么**（而非你拥有什么）。
- **服务对象**：新入驻开发者（上手）、外部/第三方集成方（消费）、平台运营（能力/文档配置）。
- **与工作台的关系**：工作台是「个人操作台」（我的资源/任务），开发者中心是「平台能力入口」（顶层菜单）。两者**并列**于顶导，互不嵌套。

---

## 2. 功能边界：工作台 vs 开发者中心

| 维度 | 工作台（现有） | 开发者中心（本文档） |
|---|---|---|
| 视角 | 我的（owned） | 平台的（available） |
| 核心动作 | 创建 / 训练 / 部署 / 管理 | 发现 / 学习 / 调用 / 连接 |
| 使用者 | 已入驻开发者 | 新人 + 外部消费者 |
| 含 Notebook / 镜像 / 部署 | ✅ 是 | ❌ 不重复（仅入口跳转） |
| API / SDK 消费 | ❌ | ✅ |
| 上手引导 / 教程 | ❌ | ✅ |
| 社区 / 创空间 | ❌（不做） | ❌（与模型广场重叠） |

**原则：操作在我，赋能在外** —— 凡是工作台已有的运营功能，开发者中心只做入口跳转 / 数据聚合，绝不重建。

**明确边界（镜像 / Notebook 归属，防止功能在两大模块间回流）：**

| 功能 | 归属 | 开发者中心是否重建 |
|---|---|---|
| 镜像上传 / 版本管理 / 删除 | 工作台·开发中心 | ❌ 不重建 |
| Notebook 实例管理 / 打开 IDE / 启停 | 工作台·开发中心 | ❌ 不重建 |
| 平台基础镜像目录（只读选型） | 可放开发者中心 | ✅ 只读展示（能力发现） |
| 示例 / 模板 Notebook + 一键启动 | 可放开发者中心 | ✅ 入口跳转（上手引导），动作落回工作台·开发中心 |

> 工作台·开发中心已具备完整的镜像上传表单与 Notebook 列表 / IDE；开发者中心若再建一份即造成「双真理源」。故开发者中心对镜像与 Notebook **只做橱窗与入口**，实际操作一律回到工作台·开发中心。

---

## 3. 信息架构（IA）

**顶层菜单映射（与现有项目顶导一一对应）：**

| 顶导位置 | 一级菜单项 | 状态 |
|---|---|---|
| 1 | 首页 | 已有 |
| 2 | 模型广场 | 已有 |
| 3 | 案例中心 | 已有 |
| 4 | 数据管理 | 已有 |
| 5 | **开发者中心** | ✅ **新增（本模块）** |
| 6 | 需求市场 | 已有 |

> 文档中心**不再**作为一级菜单项；它内嵌于开发者中心，作为左侧菜单的「文档中心」视图（见下）。原 7 项顶导缩减为 6 项。

> 工作台顶导中原有被注释的 `<span data-nav="dev">开发中心</span>`（`workbench.html:930`），**开发者中心即该预留位的正式落地**——取消注释并改名为「开发者中心」即可接入，无需新增顶导结构。

其下子树（点击顶导「开发者中心」后，左侧侧边栏展示）：

```
开发者中心
├─ 概览（首页）
├─ 快速开始（上手引导）
├─ API 中心
│   ├─ 能力目录
│   ├─ API 密钥
│   ├─ API 调试台（Explorer）
│   └─ 代码示例
├─ SDK & 工具
├─ 文档中心（内嵌 doc-center.html，去除顶部菜单，仅留文档树 + 内容 + TOC）
└─ 个人凭证（密钥 / 配额 / 权限 / 通知偏好）
```

> 「文档中心」视图以 `iframe` 方式嵌入 `doc-center.html?embed=1`：文档中心自身隐藏顶部菜单（`.app-header`）与 footer，仅保留其左侧文档树 / 正文 / 右侧 TOC；文档内容、搜索、TOC 全部复用文档中心现有实现，**不重复建设**。开发者中心左侧菜单即该视图的入口。

> 侧边栏仅显示当前顶导模块的**子项**（遵循 CLAUDE.md 侧边栏约定）；切换顶导其他菜单时开发者中心侧边栏收起，保持与现有页面一致的交互。

---

## 4. 功能模块详述（共 5 个）

### 4.1 概览（开发者中心首页）
- **目标**：一屏看清「平台能做什么 + 我该从哪开始」。
- **内容**：
  - 平台能力速览：复用工作台「平台概况」统计（187 模型 / 42 数据集 / 23 需求）+ 已开放 API 数。
  - 基础镜像目录（只读）：展示平台提供的基础镜像（CUDA / PyTorch / 农业框架等），供开发者选型；仅展示，**上传 / 管理回到工作台·开发中心**。
  - 示例 Notebook 快速启动：展示农业示例笔记本（如「作物病害识别 Notebook」），一键跳回工作台·开发中心打开 / 运行，**不重建 Notebook IDE**。
  - 我的凭证概览：已创建密钥数、本月调用量（均为复用工作台已有数据的聚合展示，不新建积分/权益逻辑）。
  - 推荐路径：根据角色（算法 / 应用 / 集成）推荐「快速开始」入口。
- **与工作台关系**：聚合展示，不新建数据。

### 4.2 快速开始（上手引导）
- **目标**：将「首次成功」时间从小时级降到分钟级。
- **内容**：
  - 场景化路径卡：① 调用一个作物识别模型 ② 微调自己的模型 ③ 发布推理服务并拿到 API ④ 把模型嵌进自有系统。
  - 每路径含：前置条件 → 步骤 → 可复制代码片段 → 跳转工作台对应功能。含「启动一个示例 Notebook」路径：展示农业示例笔记本，一键跳回工作台·开发中心打开 / 运行，不重建 Notebook IDE。
- **与工作台关系**：引导页，动作落到工作台（创建模型 / 启动 Notebook / 发布推理服务）。

### 4.3 API 中心（核心新增）
- **目标**：让外部系统消费洛书能力，**无需进入工作台** —— 这是开发者中心最具差异化的模块。
- **子模块**：
  - **能力目录**：列出所有可调用能力（模型推理 API、知识库问答 API 等），标注协议 / 鉴权 / 配额。复用「模型广场」已发布服务元数据。
  - **API 密钥**：创建 / 轮换 / 禁用密钥、绑定权限 scope、查看调用统计。
  - **API 调试台（Explorer）**：在线填参 → 发起请求 → 看响应（类阿里云 API Explorer / Stripe）。
  - **代码示例**：按语言（Python / Java / Go / cURL）生成调用片段。
- **与工作台关系**：读取工作台「我的模型 → 发布」已生成的推理服务端点（7 段发布流程产物），**不重复**发布逻辑。模型广场已发布的推理服务本身即可作为可试玩的 Demo，无需额外「创空间」。

### 4.4 SDK & 工具
- **目标**：降低集成成本。
- **内容**：官方多语言 SDK（Python / Java / Go）、CLI、客户端库的安装与初始化示例。
- **与工作台关系**：纯文档/下载，无运营功能。

### 4.5 文档中心（内嵌）
- **目标**：知识赋能，且作为平台唯一文档源。
- **内容**：直接内嵌 `doc-center.html`（通过 `?embed=1` 隐藏其自身顶部菜单与 footer），包含接入指南、Cookbook（农业场景配方）、示例项目、API 参考、常见问题等完整文档，支持搜索与右侧 TOC。
- **与文档中心关系**：**不重复建设**——开发者中心仅提供入口（左侧菜单项「文档中心」+ 全屏 iframe 视图），文档的维护仍集中在 `doc-center.html` 一处。

### 4.6 个人凭证（调用凭证管理）
- **目标**：集中管理「你用什么凭证调用平台能力」。
- **内容**：
  - **API 密钥**：列表 / 创建 / 轮换 / 禁用、绑定权限 scope、查看调用统计（与 4.3 API 密钥联动）。
  - **配额 / 计费**：当前调用量、额度上限、用量趋势。
  - **通知偏好**：调用告警、配额预警的订阅设置。
- **与工作台关系**：纯凭证/配置管理，不触碰「账号认证」与「积分权益」。
- **范围外（明确不放开发者中心）**：
  - 账号认证（个人实名 / 机构认证）→ 归入**账号设置**模块处理。
  - 积分与等级权益（1000 积分、3 次免费调用、积分规则）→ 沿用**工作台**现有体系，开发者中心不重建、不展示。

---

## 5. 与工作台 / 现有模块映射表

| 开发者中心模块 | 复用（工作台/现有） | 新增 |
|---|---|---|
| 概览 | 平台概况统计 | 凭证概览聚合（密钥/调用量） |
| 快速开始 | 创建/发布/Notebook 入口 | 场景化引导编排 |
| API 中心 | 已发布推理服务端点（模型广场） | 密钥 / 调试台 / 示例 |
| SDK & 工具 | — | 全新增 |
| 文档中心 | doc-center.html（内嵌 `?embed=1`） | 去除顶部菜单，作为开发者中心视图；内容 / 搜索 / TOC 复用 |
| 个人凭证 | — | 密钥 / 配额 / 权限 / 通知偏好 |
| 镜像 / Notebook 管理 | 工作台·开发中心（已有） | 开发者中心仅「基础镜像目录（只读）+ 示例 Notebook 启动入口」，不重建管理 |

---

## 6. 分阶段落地建议

- **P0（先做，验证价值）**：开发者中心首页 + 快速开始 + **API 中心**（密钥 + 调试台 + 示例）。这是「外部消费」的硬需求，且工作台完全没有。
- **P1**：SDK & 工具 + 文档中心（内嵌 doc-center.html）+ 个人凭证（密钥 / 配额 / 权限 / 通知偏好）。

> 无 P2：社区、应用创空间本期不做；账号认证归账号设置、积分权益沿用工作台，均不纳入开发者中心。

---

## 7. 设计约束（沿用项目规范）

- 主色 `#2f54eb`，强调色 `#23ada4`；卡片标题 2px × 14px 左 accent bar（`border-radius:1px`，`margin-right:8px`）。
- 状态 chip 规范同 `规范文件/DESIGN_SYSTEM.md`（绿=成功 / 蓝=构建中 / 灰=未构建 / 红=失败 / 青=已发布）。
- 顶导「开发者中心」与现有一级菜单平级；侧边栏仅显示当前顶导模块子项。
- 交互遵循 CLAUDE.md：简单确认用 Popconfirm，复杂选择用居中 Modal；禁用原生 `confirm()`。
